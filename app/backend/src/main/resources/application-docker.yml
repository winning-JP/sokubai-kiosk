spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:db}:3306/${DB_NAME:kiosk}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Tokyo
    username: ${DB_USER:root}
    password: ${DB_PASSWORD:root}
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
